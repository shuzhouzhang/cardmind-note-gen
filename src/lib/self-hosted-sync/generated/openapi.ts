/**
 * Placeholder replaced by `pnpm sync:self-hosted-schema` from the server's
 * canonical /openapi.json before a protocol release is cut.
 *
 * Runtime code intentionally does not duplicate endpoint shapes here. The
 * checked-in protocol facade remains the compatibility boundary while the
 * generated paths type is refreshed during protocol freeze.
 */
export type paths = Record<string, never>
